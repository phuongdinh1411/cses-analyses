{
  "buildCommand": "cd react-app && npm install && VITE_BASE_PATH=/ npm run build",
  "outputDirectory": "react-app/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
